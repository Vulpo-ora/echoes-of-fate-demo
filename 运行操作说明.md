---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: api/coze_claw/agent/file/content
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: ""
    ReservedCode2: ""
---
# 《命定·回响》运行 / 操作说明

> 知乎黑客松 · AI 游戏赛道参赛 Demo · 面向**评委演示**与**玩家试玩**
> 最后更新：2026-09-15 ｜ 本文档是把「前端页面 + 后端服务」串起来跑通的唯一操作指南
> 本次更新新增：**全局点击星光特效**、**入场星轨衔接主页**、**按故事生成人物立绘接口**（详见 [第 1.4 节](#14-本次美工与动画升级点击星光--入场星轨--立绘)）

---

## 〇、30 秒速览（先看这里）

**本 Demo 分两种运行模式，任选其一：**

| 模式 | 需要做什么 | 适合谁 | 是否要 API Key |
|------|-----------|--------|---------------|
| **A. 离线演示模式** | 只开前端页面，直接点 | 评委快速看流程 / 无网络环境 | **不需要** |
| **B. 真实 AI 模式** | 先启动后端，再开前端并配置后端地址 | 完整体验 AI 实时对话 | 需要（OpenAI 兼容 Key） |

- **只想看交互流程走一遍** → 走 **模式 A**，跳到 [第二章](#二模式a离线演示最快)。
- **要体验真正的 AI 实时剧情** → 走 **模式 B**，跳到 [第三章](#三模式b真实-ai-完整体验)。

---

## 一、项目结构与加载关系

### 1.1 目录结构（关键文件）

```
2026-09-13-f4ad8622/
├─ 运行操作说明.md                 ← 本文档
├─ backend/                        ← 后端 AI 服务（Python / FastAPI）
│  ├─ app.py                       ← 服务入口，6 个接口
│  ├─ dialogue_engine.py           ← 对话引擎（组装 Prompt / 调模型 / 判结局）
│  ├─ rag_engine.py                ← 真实情感语料 RAG（每轮 ≥10 条）
│  ├─ salt_story_engine.py         ← 盐选故事分析 / 人设 / 推荐回答
│  ├─ portrait_engine.py           ← 【新增】按故事+人物生成立绘（/portrait，失败静默降级）
│  ├─ emotion_corpus.json          ← 真实情感语料库（35 条）
│  ├─ requirements.txt             ← Python 依赖
│  └─ README.md                    ← 后端单独说明
└─ game_config/
   ├─ character_profile.py         ← 角色档案 + System Prompt（被后端 import）
   └─ assets/                      ← 前端页面（纯静态 HTML）
      ├─ start_screen.html         ← ① 开场星轨动画页（播完自动跳 index.html）
      ├─ index.html                ← ② 开局流程页（选世界观 / 建人设 / 选角色，含星轨背景）
      ├─ chat.html                 ← ③ 对话页（聊天 / 推荐回答 / 结局 / 左上角立绘）
      └─ starfx.js                 ← 【新增】全局点击星光特效（三页共用）
```

### 1.2 页面加载顺序（前端三页怎么串）

```
start_screen.html  →  index.html  →  chat.html
   开场星轨动画          开局流程          正式对话
(点"进入"/6.5秒          (走完流程后           (读取会话数据
  自动跳转 index)         自动跳转)           开始聊天)
```

> ✅ 已打通：`start_screen.html` 点击「进入星轨」或约 6.5 秒后，星轨动画收束并**自动跳转到 `index.html`**，星轨 → 主页 → 世界观三选项串联成一条完整入场链路。若想跳过片头，也可直接从 `index.html` 开始（它首屏自带旋转星轨背景）。

### 1.3 前后端是怎么连起来的（关键机制）

前端三页都是**纯静态 HTML**，通过浏览器 `localStorage` 里的两个键与后端联动：

| localStorage 键 | 作用 | 取值 |
|-----------------|------|------|
| `EOF_API` | 后端服务地址 | **留空 = 离线演示**；填 `http://127.0.0.1:8000` = 走真实后端 |
| `EOF_SESSION` | 开局流程生成的会话数据 | 由 `index.html` 自动写入，`chat.html` 读取，无需手动设置 |

**这就是模式 A / B 的开关**：`EOF_API` 空就是离线，填了后端地址就是真实 AI。

---

### 1.4 本次美工与动画升级：点击星光 / 入场星轨 / 立绘

本轮为提升演示质感，新增三项能力，**均已本地验证，且全部做了降级保护，不影响原有流程**。

#### ① 全局点击星光特效（`starfx.js`）

- **效果**：页面上每一次鼠标点击 / 触摸，都会在点击位置迸发一簇星光（五角星 + 星尘，带光晕、飘散、旋转、淡出）。
- **规则**：只监听指针事件（`pointerdown`），**不监听键盘**——所以在输入框里打字**不会**触发星光，点击按钮 / 卡片 / 选项 / 空白处都会有。这正是"除打字外每次点击都有星光"。
- **接入方式**：三个页面都已在 `</body>` 前加了一个透明的 `<canvas id="sparkLayer">`（`pointer-events:none`，不挡任何点击）并引入 `<script src="starfx.js">`。`starfx.js` 与三页放在同一目录 `game_config/assets/`，缺文件时不报错、仅无特效。
- **性能**：无点击时动画循环自动停止（不空转），限制 devicePixelRatio ≤ 2，低端设备也流畅。

#### ② 入场星轨动画衔接主页

- `start_screen.html` 首屏是旋转星轨 + 汇聚流星动画，点击「进入星轨」或约 6.5 秒后**自动跳转到 `index.html`**（原先只停在占位层，现已打通）。
- `index.html` 首屏（S0）额外加了一层**旋转星轨背景**（5 条椭圆轨道 + 运行星点 + 微尘星，纯 canvas，`z-index:-1` 不挡交互），点「进入星轨」用透明度过渡进入世界观三选项。
- 整体链路：**片头星轨 → 主页星轨 → 点击进入 → 盐选 / 现实 / 返回三选项**。

#### ③ 按故事生成人物立绘（后端 `/portrait` + 聊天页左上角头像）

- **后端**：新增 `portrait_engine.py`，复用已配置的 OpenAI 兼容图像接口（`images.generate`）。盐选线按"故事 + 角色人设 / 外貌"、现实线按"性别 + 职业"组装提示词生成一张立绘；带**进程内缓存**（同角色不重复计费）。`app.py` 已注册 `POST /portrait` 路由。
- **前端**：`chat.html` 进入对话前调用 `/portrait`，成功则把立绘填入**左上角头像**，并在背景叠一层很弱的人物光晕（不喧宾夺主）。
- **降级**：图像接口不可用 / 超时 / 报错时，`/portrait` 返回空 URL，前端自动显示**默认微光头像**，**对话完全不受影响**（立绘请求与开场白并行，不阻塞聊天）。
- **可配置**：默认图像模型 `dall-e-3`。若你的 Key 用别的图像模型，在后端环境变量加 `IMAGE_MODEL=模型名`（可选 `IMAGE_SIZE`，默认 `1024x1024`），**无需改代码**。若 Key 不支持图像生成，保持默认即走降级，聊天照常。

> 部署包已随本轮改动重新生成：`echoes-of-fate-api-deploy.zip`（后端，含 `portrait_engine.py`）、`frontend-github-pages.zip`（前端，含 `starfx.js` 与三页最新版）。

---

## 二、模式 A：离线演示（最快）

**特点**：无需后端、无需 Key、无需网络。前端按内置脚本回放一段完整交互，用来给评委快速看清「开局流程 → 对话 → 选择节点 → 结局」的完整体验。

### 步骤

1. **启动一个本地静态服务器**（推荐，避免浏览器对本地文件的跨页限制）。
   在 `game_config/assets/` 目录下打开终端，执行：
   ```bash
   # 任选其一
   python -m http.server 5500        # Python 自带
   npx serve .                       # 或用 Node 的 serve
   ```
   然后浏览器访问：`http://127.0.0.1:5500/index.html`

   > 也可直接双击 `index.html` 用浏览器打开，但个别浏览器对 `file://` 下的页面跳转/存储更严格，建议优先用本地服务器方式。

2. **确认处于离线模式**：离线是默认状态，不需要任何设置（`EOF_API` 默认为空）。
   如果之前配置过后端想切回离线，按 F12 打开控制台执行：
   ```js
   localStorage.removeItem('EOF_API');
   ```

3. **走开局流程**（`index.html`）：
   - 点「进入星轨」→ 选世界观：
     - **盐选故事**：输入一个故事名（如"错睡替身"）→ 选性别 + 起名 → AI 分析（离线用内置示例）→ 选代入角色 → 选陪伴对象 → 进入对话。
     - **现实世界**：选双方性别 + 填职业 + 起名 → 直接进入对话。

4. **体验对话**（`chat.html`）：
   - 在底部输入框随便说点什么，点「发送」——离线模式会**按内置剧本依次回放** 5 段剧情（含内心 OS、场景切换、选择节点，最后触发「救赎结局」）。
   - 点「💡 帮我想」会弹出 3 条可编辑推荐回答（离线为内置示例）。

### 离线模式的边界（演示时如实说明）

- 对话内容是**预置脚本回放**，不随玩家输入真正变化——它证明的是"交互流程与 UI 完整"，不是 AI 实时生成能力。
- 想展示"你说什么故事就往哪走"的真实 AI 能力，请走模式 B。

---

## 三、模式 B：真实 AI 完整体验

**特点**：真正调用大模型，AI 实时理解玩家输入、推演剧情、更新隐藏数值、判定结局，每轮强制注入 ≥10 条真实情感语料。

### 3.1 前置条件

- 已安装 **Python 3.9+**。
- 一个 **OpenAI 兼容 API Key**（项目默认走 openai-next 中转，也可用官方 OpenAI）。

### 3.2 第一步：启动后端服务

在 `backend/` 目录下打开终端：

```bash
# 1) 安装依赖
pip install -r requirements.txt

# 2) 配置环境变量（把 你的key 换成真实 Key）
#    Windows PowerShell：
$env:OPENAI_API_KEY="你的key"
$env:OPENAI_BASE_URL="https://api.openai-next.com/v1"
$env:CHAT_MODEL="gpt-4o"
#    macOS / Linux：
export OPENAI_API_KEY=你的key
export OPENAI_BASE_URL=https://api.openai-next.com/v1
export CHAT_MODEL=gpt-4o

# 3) 启动服务
uvicorn app:app --host 127.0.0.1 --port 8000
```

看到 uvicorn 提示监听 `127.0.0.1:8000` 即启动成功。

**验证后端健康**（另开一个终端）：
```bash
curl http://127.0.0.1:8000/health
# 期望返回：{"ok":true,"model":"gpt-4o"}
```

> 🔒 安全提示：API Key 只写进本机环境变量，**不要**写进任何前端文件或提交到仓库。

### 3.3 第二步：启动前端并连接后端

1. 在 `game_config/assets/` 目录启动静态服务器（同模式 A 步骤 1）：
   ```bash
   python -m http.server 5500
   ```
   访问 `http://127.0.0.1:5500/index.html`。

2. **配置后端地址**：按 F12 打开浏览器控制台，执行：
   ```js
   localStorage.setItem('EOF_API', 'http://127.0.0.1:8000');
   ```
   刷新页面后即进入真实 AI 模式。（想切回离线：`localStorage.removeItem('EOF_API')`）

3. 之后开局流程与对话操作与模式 A 相同，但此时：
   - 盐选故事分析走后端 `/analyze_story`（真实梳理人物与剧情）；
   - 每轮对话走 `/chat`，AI 实时生成回复、内心 OS、场景、数值与结局；
   - 「💡 帮我想」走 `/suggest`，实时生成 3 条贴合当前剧情的可编辑回答。

### 3.4 后端接口一览（供联调 / 排障）

| 方法 | 路径 | 作用 | 前端调用页 |
|------|------|------|-----------|
| POST | `/new_game` | 按性别/姓名/世界观/问卷生成角色档案+初始四数值 | （问卷线预留） |
| POST | `/chat` | 每轮对话：回复+四数值增量+阶段+结局判定 | chat.html |
| POST | `/analyze_story` | 盐选线：分析故事，返回可代入角色/陪伴角色/剧情脉络 | index.html |
| POST | `/salt_persona` | 盐选线：确认所选人物，返回 system_prompt+初始数值 | chat.html |
| POST | `/suggest` | 生成 3 条可编辑推荐回答 | chat.html |
| POST | `/portrait` | 【新增】按故事+人物生成立绘，失败静默降级 | chat.html |
| GET  | `/health` | 健康检查，返回当前模型名 | 手动验证 |

> 说明：后端实际接口比 `backend/README.md` 更全——README 仅列了 `/new_game`、`/chat`、`/health`，而 `app.py` 另外实现了 `/analyze_story`、`/salt_persona`、`/suggest`、`/portrait`，前端已在使用。`/portrait` 依赖图像模型（默认 `dall-e-3`，可用环境变量 `IMAGE_MODEL` 覆盖）；不可用时自动降级为默认头像，不影响对话。

---

## 四、核心机制速记（演示话术用）

- **隐藏四数值**：友情值 / 爱情值 / 宿命值 / 遗憾值（0–100，全隐藏，无进度条）。
- **三结局**：救赎 / 幸福 / 遗憾，由 AI 按全程行为自动判定，玩家不直接选结局。
- **现实世界观** `has_ending=false`，为纯陪伴模式，不触发结局。
- **真实语料壁垒**：每轮对话强制注入 ≥10 条源自知乎高赞情感帖的真实语料（RAG）。
- **低虐/零误会**：冲突只来自外部事件，角色主动沟通，全程调性不崩坏。

---

## 五、常见问题（FAQ）

**Q1：打开页面点发送没反应 / 报连接异常？**
- 若在真实模式：确认后端已启动（`curl /health` 通），且 `EOF_API` 填的是 `http://127.0.0.1:8000`（注意 http、端口、无多余斜杠）。
- 后端跨域已开放（CORS `allow_origins=["*"]`），一般无需额外配置。

**Q2：对话内容不随我说的变化？**
- 说明当前是**离线模式**（`EOF_API` 为空），走的是内置剧本。要真实 AI 请按第三章配置后端地址。

**Q3：`start_screen.html` 打开后卡在动画、进不去？**
- 它是开场星轨页，点击「进入星轨」或等约 6.5 秒会自动跳转到 `index.html`；也可直接从 `index.html` 开始（首屏自带星轨背景）。

**Q3b：点击没有星光 / 打字时也想要星光？**
- 星光靠 `starfx.js`，确认它与 HTML 在同一目录（`game_config/assets/`）且已随页面加载。打字**故意不触发**星光（只响应鼠标/触摸点击），这是设计如此；点击任意位置即可看到。

**Q3c：聊天页左上角没有人物立绘、只有一个微光圆点？**
- 说明当前 `/portrait` 走了降级：可能是离线模式（`EOF_API` 为空）、Key 不支持图像生成、或图像接口超时。**这不影响对话**。想启用立绘：走真实模式 + 使用支持图像生成的 Key，必要时用环境变量 `IMAGE_MODEL` 指定可用的图像模型。

**Q4：后端启动报 `ModuleNotFoundError` / import 失败？**
- 确认在 `backend/` 目录启动，且已 `pip install -r requirements.txt`；后端会自动把同目录与 `../game_config` 加入路径以 import `character_profile`。

**Q5：curl /health 返回 200 但对话报模型错误？**
- 检查 `OPENAI_API_KEY` 是否有效、`OPENAI_BASE_URL` 与 Key 是否匹配、`CHAT_MODEL` 模型名是否可用。

**Q6：想快速在离线/真实之间切换？**
```js
// 切真实
localStorage.setItem('EOF_API','http://127.0.0.1:8000');
// 切离线
localStorage.removeItem('EOF_API');
// 清空开局会话（重新走流程）
localStorage.removeItem('EOF_SESSION');
```

---

## 六、演示推荐路径（评委 5 分钟）

1. 用 `start_screen.html` 播 6 秒星轨片头（点「进入星轨」自动衔接主页），讲一句项目定位。
2. 打开 `index.html` 走一遍**盐选故事**开局：输入"错睡替身" → 起名 → 选代入角色 → 选陪伴对象 → 进入对话。（过程中点任意按钮/卡片都会迸发**星光**，是可视亮点）
3. 在 `chat.html` 连发几条，展示：左上角**人物立绘**、内心 OS、场景切换、选择节点弹窗、「💡 帮我想」推荐回答，直到触发**救赎结局**。
4. 若现场网络允许，切到真实模式（配置 `EOF_API` + 启动后端），现场自由输入一句，展示 AI 真正的实时生成能力（含按故事生成的立绘）。

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
